<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#ffffff"
    tools:context="com.example.hasee.taiheapp.activity.WebViewActivity">

    <ImageView
        android:id="@+id/backImageView"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:padding="7dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:src="@drawable/toolbar_back"
        />

    <TextView
        android:id="@+id/titleTextView"
        android:textColor="#000000"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:gravity="center"
        app:layout_constraintLeft_toRightOf="@+id/backImageView"
        app:layout_constraintTop_toTopOf="@+id/backImageView"
        app:layout_constraintBottom_toBottomOf="@id/backImageView"
        android:text="标题"
        />

    <TextView
        android:id="@+id/refreshTextView"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:textColor="#000000"
        android:text="刷新"
        android:gravity="center"
        app:layout_constraintRight_toRightOf="parent"
        />

    <WebView
        android:id="@+id/webView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="50dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/titleTextView"></WebView>

</android.support.constraint.ConstraintLayout>
